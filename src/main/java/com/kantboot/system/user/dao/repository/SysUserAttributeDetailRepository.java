package com.kantboot.system.user.dao.repository;

import com.kantboot.system.user.domain.entity.SysUserAttributeDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SysUserAttributeDetailRepository extends JpaRepository<SysUserAttributeDetail, Long> {
}